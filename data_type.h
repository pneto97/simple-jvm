typedef enum data_type { 
    BYTE,
    SHORT,
    INT,
    LONG,
    CHAR,
    FLOAT,
    DOUBLE,
    RETURN_ADDR, 
    CLASS,
    ARRAY,
    INTERFACE,
    NULL,
} data_type;

typedef enum cat_type { 
    UNIQUE,
    FIRST,
    SECOND,
} cat_type;